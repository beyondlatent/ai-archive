# AGENT.md

이 저장소에서 작업할 때 따르는 지시사항과 원칙입니다. 저장소 소개와 수록된 글 목록은 [README.md](README.md)에 있습니다.

---

## 1. 이 저장소가 하는 일

사용자가 관심 있게 읽은 **보고서 · 기사 · 블로그 글**을 채팅으로 하나씩 요청하면, 원문을 조사해 한국어로 정리하고 정적 웹페이지로 추가합니다.

원문을 다시 찾지 않아도 핵심을 되짚을 수 있도록, 요약뿐 아니라 **원문 링크 · 조사 방법 · 핵심 수치 · 시사점 · 읽을 때 유의할 점**까지 함께 적습니다. 숫자가 비슷해 보여도 측정 기준이 다르면 그 차이를 짚어 둡니다.

사용자는 앞으로도 채팅으로 신규 글 정리를 계속 요청합니다. 매번 같은 구조와 같은 품질로 추가하는 것이 이 저장소의 목표입니다.

---

## 2. 저장소 구조

```
docs/
  index.html          목록 페이지 (검색 · 태그 필터)
  assets/
    style.css         공통 스타일 (라이트 · 다크 테마)
    site.js           테마 전환, 목록 검색과 필터
  posts/
    _TEMPLATE.html    새 글 템플릿
    *.html            글 하나당 파일 하나
AGENT.md              이 문서
README.md             저장소 소개와 수록된 글 목록
```

빌드 도구나 의존성이 없는 정적 HTML입니다. 로컬에서 확인하려면 `docs/index.html`을 브라우저로 엽니다.

새 글을 추가할 때 손대는 파일은 셋입니다. `docs/posts/`에 새 HTML 하나, `docs/index.html`의 카드 목록, `README.md`의 수록 글 표입니다.

### 글의 세 계층

같은 주제를 다루는 글이 여러 편일 때 세 계층으로 나눕니다.

| 계층 | 배지 | 다루는 범위 | 파일명 |
| --- | --- | --- | --- |
| **통합 전망** | 🔵 파랑 | 여러 기관을 가로지름 | `YYYY-YYYY-주제.html` |
| **기관 종합** | 🟢 초록 | 한 기관의 여러 보고서 | `YYYY-YYYY-기관-주제.html` |
| **개별 보고서** | 🟠 주황 | 보고서 한 편 | `YYYY-MM-기관-보고서슬러그.html` |

계층은 화면에도 드러나야 합니다. 목록 페이지는 세 구역으로 나뉘고, 글 페이지 머리말과 카드에는 계층 배지가 붙습니다. 색은 `docs/assets/style.css`의 `--layer-*` 토큰에서 관리합니다.

규칙은 이렇습니다.

- **보고서 하나는 문서 하나입니다.** 여러 보고서를 한 문서에 몰아넣지 않습니다.
- **기관 종합 페이지는 보고서가 둘 이상일 때만** 만듭니다. 한 편뿐이면 개별 보고서 페이지가 그 역할을 겸합니다.
- 통합 페이지의 각 보고서 절에는 **`→ 개별 정리 보기` 링크**를 답니다.
- 통합 전망 페이지는 기관별 서술을 반복하지 않고 **비교와 차이에만** 집중합니다.

### 정렬은 원문 발행일 기준

목록과 README 모두 **원문 발행일 최신순**입니다. 저장소에 언제 추가했는지가 아니라 보고서가 언제 나왔는지가 기준입니다.

- 여러 보고서를 묶은 페이지(기관 종합, 통합 전망)는 **그 안에서 가장 늦게 나온 보고서의 발행일**을 씁니다.
- 발행 월이 공개되지 않은 보고서는 추정한 값을 쓰되 **왜 그 달로 보았는지 근거를 남깁니다.**
- 카드에는 `data-pubdate="YYYY-MM"` 속성으로 적습니다. README에는 항목 앞에 `` `YYYY-MM` `` 으로 표기합니다.
- README의 기관 순서도 그 기관의 가장 늦은 보고서 발행일을 따릅니다.

---

## 3. 작업 원칙

### 원문 링크를 반드시 남긴다
공식 경로를 맨 위에 두고, 그 아래에 PDF 미러와 보도 링크를 덧붙입니다. 공식 배포가 중단됐거나 유료라면 **그 사정을 각주로 적습니다.** 링크 없는 요약은 이 저장소에서 미완성으로 취급합니다.

### 수치는 표나 수치 카드로
본문 문장 안에 숫자를 흩뿌리지 않습니다. 비교할 값이 둘 이상이면 표, 대표 지표 서너 개는 `.stat-grid` 카드를 씁니다.

### 한계를 반드시 같이 적는다
모든 글은 **"읽을 때 유의할 점"** 섹션으로 끝납니다. 최소한 다음을 점검합니다.

- 표본 크기와 대표성
- 자기보고 설문인지, 실측 데이터인지
- 발행 주체의 이해관계 (컨설팅사가 자사 서비스 영역을 처방하는 등)
- 동료평가 여부
- 다른 보고서와 숫자가 비슷해 보여도 **측정 기준이 다른 경우**

### 숫자를 기관 간에 함부로 비교하지 않는다
표본과 정의가 다르면 같은 수치라도 다른 것을 센 값입니다. 비교표를 만들 때는 정의가 다르다는 점을 본문에 명시합니다.

### 핵심 용어는 영문을 병기한다
`학습 격차(learning gap)`처럼 처음 나올 때 한 번 병기합니다.

### 발행 주체의 성격을 분류해 읽는다

같은 주제라도 누가 조사했느냐에 따라 수치가 크게 갈립니다. 실제로 이 저장소의 '성과를 낸 조직' 비율은 5%에서 97%까지 벌어집니다. 값을 가르는 것은 세 가지입니다.

- **측정 단위** — 전사 EBIT 기여인가, 개별 사용 사례의 수익인가
- **응답자 층** — CEO인가, AI 도입을 주도하는 실무 리더인가
- **표본 모집 방식** — 무작위인가, 이미 AI에 투자한 조직인가

특히 **AI 제품이나 인프라를 파는 기업의 조사는 수치가 일관되게 높습니다.** 컨설팅사, 대학, 국제기구, 기술기업, 벤처캐피털을 구분해 적고, 이해관계가 다른 기관이 같은 결론에 닿는 지점을 따로 짚습니다.

### 출처가 불확실하면 쓰지 않는다
검색 결과에서 판본이 섞여 나오는 수치(예: 연도가 다른 조사의 값)는 버리거나, 어느 판본의 값인지 명시합니다. 추측으로 채우지 않습니다.

---

## 4. 새 글 추가 절차

1. **원문을 조사한다.** 공식 링크, 발행일, 저자, 조사 방법, 핵심 수치를 확보합니다.
2. **어느 계층인지 정한다.** 2번 절의 표를 따릅니다.
3. **발행일을 정한다.** 원문 발행 연·월(`YYYY-MM`). 파일명 앞부분과 같아야 합니다. 월이 공개되지 않았다면 어떤 근거로 그 달을 골랐는지 메모합니다.
4. **`docs/posts/_TEMPLATE.html`을 복사한다.** 파일명은 계층에 맞게 짓습니다.
5. **`{{ }}` 자리표시자를 모두 채운다.** 섹션 순서는 5번 절을 따릅니다.
6. **`docs/index.html`의 해당 계층 구역에 카드를 추가한다.** 같은 구역 안에서 **발행일 최신순 위치**에 넣습니다.
   - 구역은 `#post-groups` 안의 `.layer-group` 셋 중 하나입니다.
   - `data-tags` — 필터용. **소문자, `|` 구분.** 첫 값은 계층이며 **화면 표기와 정확히 같아야** 합니다 (예: `개별 보고서|ai 도입|roi`). 계층 메뉴의 `data-layer-filter` 값과 다르면 그 계층이 0편으로 잡힙니다.
   - `data-pubdate` — 정렬용. `YYYY-MM` 형식의 원문 발행일
   - `data-search` — 검색용. 한국어와 영어 키워드를 모두 넣습니다.
   - 카드와 글 페이지 머리말에 `<span class="tag tag-layer" data-layer="...">` 배지를 답니다.
7. **글 수를 갱신한다.** `#doc-count`와 `#result-count`의 기본값, 그리고 해당 구역 `.layer-head .count`를 함께 고칩니다. (필터를 걸면 구역 편수는 스크립트가 다시 계산합니다.)
8. **상호 링크를 건다.**
   - 기관 종합 페이지가 있으면 해당 절에 `→ 개별 정리 보기` 링크를 답니다.
   - 관련 있는 기존 글의 "함께 보기"에도 새 글을 추가합니다.
9. **`README.md`에 항목을 추가한다.** 기관 묶음 안에서 발행일 최신순 위치에 넣고, 기관 순서도 그 기관의 가장 늦은 발행일을 따릅니다.
10. **검증한다.** 7번 절.
11. **커밋하고 푸시한다.** 8번 절.

---

## 5. 글 문서 구조

모든 글은 이 순서를 따릅니다.

| 섹션 | 내용 |
| --- | --- |
| 제목 + `dek` | 두세 문장으로 이 글이 무엇을 말하는지 |
| 태그 | 유형 태그 하나 + 주제 태그 여러 개 |
| 메타 카드 | 발행 기관 · 발행일 · 저자 · 유형 · 추가일 |
| 원문 링크 박스 | 공식 링크를 맨 위에, 미러와 보도 링크를 아래에 |
| 목차 | 본문 `h2`와 1:1 대응 |
| 한눈에 보기 | `.lead` 한 문단. 핵심 주장만 |
| 조사 방법 | 표본 · 범위 · 기간 |
| 핵심 수치 | 수치 카드와 표 |
| 주요 발견 | 번호를 붙인 `h2` 여러 개 |
| 시사점 | 번호 목록. 독자가 무엇을 해야 하는지 |
| 읽을 때 유의할 점 | `.callout-warn`. **생략 금지** |
| 함께 보기 | 저장소 내 관련 글 |

---

## 6. 태그 규칙

- **계층 배지** (하나만, `.tag-layer` + `data-layer` 속성): `통합 전망` `기관 종합` `개별 보고서`
  - 색은 `data-layer` 값으로 자동 결정됩니다. 클래스를 따로 지정하지 않습니다.
  - 목록 카드와 글 페이지 머리말 모두 같은 마크업을 씁니다.
- **주제 태그** (여러 개): `AI 도입` `ROI` `에이전트` `생산성` `인력` `조직 변화` `거버넌스` `시장` `파일럿 실패`

보고서가 아닌 글(기사, 블로그)을 추가할 때는 계층 배지를 `개별 보고서` 대신 그 성격에 맞게 새로 정의하고, `style.css`에 색 토큰을 추가합니다.

목록 페이지의 분류 장치는 두 가지이고 **서로 독립된 축**입니다.

- **계층 메뉴** (`#layer-nav`) — 검색창 **위**에 있는 탭 형태 메뉴입니다. 전체 / 통합 전망 / 기관 종합 / 개별 보고서 네 값으로 고정입니다. 각 탭의 편수는 계층 선택을 뺀 나머지 조건(검색어 + 주제)으로 계산되므로, 다른 계층에 몇 편이 남았는지 보입니다.
- **주제 필터** (`.filters`) — 검색창 **아래**에 있는 칩입니다. 새 주제 태그를 노출하려면 여기에 칩 버튼을 추가합니다.

둘은 조합됩니다. `기관 종합` + `에이전트`처럼 함께 걸 수 있고, 선택된 값을 다시 누르면 해제됩니다.

---

## 7. 검증

푸시 전에 반드시 통과시킵니다.

- **HTML 구조** — 태그 짝, 중복 `id`, 깨진 내부 앵커, 깨진 파일 링크
- **목차와 본문 대응** — 목차의 모든 `#앵커`가 실제 `id`에 연결되는지
- **반응형** — 1280px · 768px · 390px에서 가로 스크롤이 없는지
- **동작** — 검색, 계층 메뉴, 주제 필터, 둘의 조합, 재클릭 해제, 계층별 편수 재계산, 빈 구역 숨김, 빈 상태, 테마 전환
- **정렬** — 각 구역이 원문 발행일 최신순인지, 모든 카드에 `data-pubdate`가 있는지
- **콘솔** — 오류 0건

브라우저 검증은 Chromium이 `/opt/pw-browsers/`에 미리 설치돼 있으므로 `playwright-core`로 띄웁니다. `playwright install`은 실행하지 않습니다.

---

## 8. Git 규칙

- 기본 브랜치는 `main`입니다.
- 커밋 메시지는 **영문**으로 씁니다. 제목은 명령형 한 줄, 본문에 무엇을 왜 바꿨는지 적습니다.
- 빌드 산출물이나 임시 파일을 작업 트리에 남기지 않습니다. `*.bundle`, 로그, 캐시는 `.gitignore`에 있습니다.
- 풀 리퀘스트는 **사용자가 명시적으로 요청할 때만** 만듭니다.

---

## 9. 사이트 규칙

2번 절의 제약(빌드 도구·의존성 없음)을 유지합니다.

- **프레임워크나 번들러를 도입하지 않습니다.** 새 글은 HTML 파일 하나를 추가하는 것으로 끝나야 합니다.
- **외부 CDN에 의존하지 않습니다.** 폰트는 시스템 스택을 씁니다.
- **스타일은 `docs/assets/style.css` 한 곳에서 관리합니다.** 글 파일에 인라인 스타일을 넣지 않습니다.
- **계층 색은 `--layer-*` 토큰으로만 관리합니다.** 라이트와 다크 양쪽에 모두 정의합니다.
- **색상은 `:root` 토큰으로 정의합니다.** 라이트/다크 양쪽을 모두 정의하고, 한쪽에만 존재하는 색을 만들지 않습니다.
- **`.wrap`과 같은 요소에 클래스를 덧붙일 때 좌우 패딩을 덮어쓰지 않습니다.** `padding` 단축 속성 대신 `padding-top` / `padding-bottom`을 씁니다.

### GitHub Pages 배포

저장소 **Settings → Pages** 에서 지정합니다.

| 항목 | 값 |
| --- | --- |
| Source | Deploy from a branch |
| Branch | `main` |
| Folder | `/docs` |

배포 주소는 https://beyondlatent.github.io/ai-archive/ 입니다.

---

## 10. 원문에 접근할 수 없을 때

실행 환경의 네트워크 정책으로 특정 도메인이 차단될 수 있습니다. 그럴 때는,

1. 다른 경로(미러 PDF, 보도자료, 공식 요약본)를 찾습니다.
2. 그래도 안 되면 **추측으로 채우지 말고** 사용자에게 상황을 알리고 방향을 묻습니다.
3. 접근하지 못한 자료를 정리한 경우, 무엇을 근거로 썼는지 글에 밝힙니다.

---

## 11. 하지 말 것

- 원문 링크 없이 요약만 올리기
- "읽을 때 유의할 점" 섹션 생략하기
- 확인되지 않은 수치를 그럴듯하게 채우기
- 판본이 다른 조사의 숫자를 한 보고서의 것으로 섞기
- 여러 보고서를 한 문서에 몰아넣기 (보고서 하나는 문서 하나)
- 발행 주체의 성격을 밝히지 않고 수치만 옮기기
- 요청하지 않은 풀 리퀘스트 만들기
- 빌드 도구나 외부 의존성 추가하기
