# AGENT.md

이 저장소에서 작업할 때 따르는 지시사항과 원칙입니다. 저장소 소개와 수록된 글 목록은 [README.md](README.md)에 있습니다.

---

## 1. 이 저장소가 하는 일

사용자가 관심 있게 읽은 **보고서 · 기사 · 블로그 글**을 채팅으로 하나씩 요청하면, 원문을 조사해 한국어로 정리하고 정적 웹페이지로 추가합니다.

원문을 다시 찾지 않아도 핵심을 되짚을 수 있도록, 요약뿐 아니라 **원문 링크 · 조사 방법 · 핵심 수치 · 시사점 · 읽을 때 유의할 점**까지 함께 적습니다. 숫자가 비슷해 보여도 측정 기준이 다르면 그 차이를 짚어 둡니다.

사용자는 앞으로도 채팅으로 신규 글 정리를 계속 요청합니다. 매번 같은 구조와 같은 품질로 추가하는 것이 이 저장소의 목표입니다.

---

## 2. 저장소 구조

```
docs/
  index.html          목록 페이지 (검색 · 태그 필터)
  assets/
    style.css         공통 스타일 (라이트 · 다크 테마)
    site.js           테마 전환, 목록 검색과 필터
  posts/
    _TEMPLATE.html    새 글 템플릿
    *.html            글 하나당 파일 하나
AGENT.md              이 문서
README.md             저장소 소개와 수록된 글 목록
```

빌드 도구나 의존성이 없는 정적 HTML입니다. 로컬에서 확인하려면 `docs/index.html`을 브라우저로 엽니다.

새 글을 추가할 때 손대는 파일은 셋입니다. `docs/posts/`에 새 HTML 하나, `docs/index.html`의 카드 목록, `README.md`의 수록 글 표입니다.

---

## 3. 작업 원칙

### 원문 링크를 반드시 남긴다
공식 경로를 맨 위에 두고, 그 아래에 PDF 미러와 보도 링크를 덧붙입니다. 공식 배포가 중단됐거나 유료라면 **그 사정을 각주로 적습니다.** 링크 없는 요약은 이 저장소에서 미완성으로 취급합니다.

### 수치는 표나 수치 카드로
본문 문장 안에 숫자를 흩뿌리지 않습니다. 비교할 값이 둘 이상이면 표, 대표 지표 서너 개는 `.stat-grid` 카드를 씁니다.

### 한계를 반드시 같이 적는다
모든 글은 **"읽을 때 유의할 점"** 섹션으로 끝납니다. 최소한 다음을 점검합니다.

- 표본 크기와 대표성
- 자기보고 설문인지, 실측 데이터인지
- 발행 주체의 이해관계 (컨설팅사가 자사 서비스 영역을 처방하는 등)
- 동료평가 여부
- 다른 보고서와 숫자가 비슷해 보여도 **측정 기준이 다른 경우**

### 숫자를 기관 간에 함부로 비교하지 않는다
표본과 정의가 다르면 같은 수치라도 다른 것을 센 값입니다. 비교표를 만들 때는 정의가 다르다는 점을 본문에 명시합니다.

### 핵심 용어는 영문을 병기한다
`학습 격차(learning gap)`처럼 처음 나올 때 한 번 병기합니다.

### 출처가 불확실하면 쓰지 않는다
검색 결과에서 판본이 섞여 나오는 수치(예: 연도가 다른 조사의 값)는 버리거나, 어느 판본의 값인지 명시합니다. 추측으로 채우지 않습니다.

---

## 4. 새 글 추가 절차

1. **원문을 조사한다.** 공식 링크, 발행일, 저자, 조사 방법, 핵심 수치를 확보합니다.
2. **`docs/posts/_TEMPLATE.html`을 복사한다.**
   - 파일명: `YYYY-MM-기관-제목슬러그.html` (예: `2026-03-openai-enterprise-report.html`)
   - 여러 보고서를 묶는 정리 글은 기간을 앞에 둡니다 (예: `2025-2026-enterprise-ai-adoption-research.html`)
3. **`{{ }}` 자리표시자를 모두 채운다.** 섹션 순서는 아래 5번을 따릅니다.
4. **`docs/index.html`의 `#post-list`에 카드를 추가한다.**
   - `data-tags` — 필터용. **소문자, `|` 구분** (예: `보고서|ai 도입|roi`)
   - `data-search` — 검색용. 한국어와 영어 키워드를 모두 넣습니다.
5. **글 수를 갱신한다.** `#doc-count`와 `#result-count`의 기본값을 함께 고칩니다.
6. **기존 글의 "함께 보기"를 갱신한다.** 관련 있는 글끼리 상호 링크를 겁니다.
7. **`README.md`의 수록 글 표에 행을 추가한다.**
8. **검증한다.** 아래 7번.
9. **커밋하고 푸시한다.** 아래 8번.

---

## 5. 글 문서 구조

모든 글은 이 순서를 따릅니다.

| 섹션 | 내용 |
| --- | --- |
| 제목 + `dek` | 두세 문장으로 이 글이 무엇을 말하는지 |
| 태그 | 유형 태그 하나 + 주제 태그 여러 개 |
| 메타 카드 | 발행 기관 · 발행일 · 저자 · 유형 · 추가일 |
| 원문 링크 박스 | 공식 링크를 맨 위에, 미러와 보도 링크를 아래에 |
| 목차 | 본문 `h2`와 1:1 대응 |
| 한눈에 보기 | `.lead` 한 문단. 핵심 주장만 |
| 조사 방법 | 표본 · 범위 · 기간 |
| 핵심 수치 | 수치 카드와 표 |
| 주요 발견 | 번호를 붙인 `h2` 여러 개 |
| 시사점 | 번호 목록. 독자가 무엇을 해야 하는지 |
| 읽을 때 유의할 점 | `.callout-warn`. **생략 금지** |
| 함께 보기 | 저장소 내 관련 글 |

---

## 6. 태그 규칙

- **유형 태그** (하나만, `.tag-type` 클래스): `보고서` `기사` `블로그` `리서치 정리`
- **주제 태그** (여러 개): `AI 도입` `ROI` `에이전트` `생산성` `인력` `조직 변화` `파일럿 실패`

새 주제 태그를 만들면 목록 상단 필터에 노출할지 판단합니다. 노출하려면 `docs/index.html`의 `.filters`에 칩 버튼을 추가합니다.

---

## 7. 검증

푸시 전에 반드시 통과시킵니다.

- **HTML 구조** — 태그 짝, 중복 `id`, 깨진 내부 앵커, 깨진 파일 링크
- **목차와 본문 대응** — 목차의 모든 `#앵커`가 실제 `id`에 연결되는지
- **반응형** — 1280px · 768px · 390px에서 가로 스크롤이 없는지
- **동작** — 검색, 태그 필터, 빈 상태, 테마 전환
- **콘솔** — 오류 0건

브라우저 검증은 Chromium이 `/opt/pw-browsers/`에 미리 설치돼 있으므로 `playwright-core`로 띄웁니다. `playwright install`은 실행하지 않습니다.

---

## 8. Git 규칙

- 기본 브랜치는 `main`입니다.
- 커밋 메시지는 **영문**으로 씁니다. 제목은 명령형 한 줄, 본문에 무엇을 왜 바꿨는지 적습니다.
- 빌드 산출물이나 임시 파일을 작업 트리에 남기지 않습니다. `*.bundle`, 로그, 캐시는 `.gitignore`에 있습니다.
- 풀 리퀘스트는 **사용자가 명시적으로 요청할 때만** 만듭니다.

---

## 9. 사이트 규칙

2번 절의 제약(빌드 도구·의존성 없음)을 유지합니다.

- **프레임워크나 번들러를 도입하지 않습니다.** 새 글은 HTML 파일 하나를 추가하는 것으로 끝나야 합니다.
- **외부 CDN에 의존하지 않습니다.** 폰트는 시스템 스택을 씁니다.
- **스타일은 `docs/assets/style.css` 한 곳에서 관리합니다.** 글 파일에 인라인 스타일을 넣지 않습니다.
- **색상은 `:root` 토큰으로 정의합니다.** 라이트/다크 양쪽을 모두 정의하고, 한쪽에만 존재하는 색을 만들지 않습니다.
- **`.wrap`과 같은 요소에 클래스를 덧붙일 때 좌우 패딩을 덮어쓰지 않습니다.** `padding` 단축 속성 대신 `padding-top` / `padding-bottom`을 씁니다.

### GitHub Pages 배포

저장소 **Settings → Pages** 에서 지정합니다.

| 항목 | 값 |
| --- | --- |
| Source | Deploy from a branch |
| Branch | `main` |
| Folder | `/docs` |

배포 주소는 https://beyondlatent.github.io/ai-archive/ 입니다.

---

## 10. 원문에 접근할 수 없을 때

실행 환경의 네트워크 정책으로 특정 도메인이 차단될 수 있습니다. 그럴 때는,

1. 다른 경로(미러 PDF, 보도자료, 공식 요약본)를 찾습니다.
2. 그래도 안 되면 **추측으로 채우지 말고** 사용자에게 상황을 알리고 방향을 묻습니다.
3. 접근하지 못한 자료를 정리한 경우, 무엇을 근거로 썼는지 글에 밝힙니다.

---

## 11. 하지 말 것

- 원문 링크 없이 요약만 올리기
- "읽을 때 유의할 점" 섹션 생략하기
- 확인되지 않은 수치를 그럴듯하게 채우기
- 판본이 다른 조사의 숫자를 한 보고서의 것으로 섞기
- 요청하지 않은 풀 리퀘스트 만들기
- 빌드 도구나 외부 의존성 추가하기
